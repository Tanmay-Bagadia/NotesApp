// Get DOM Elements

let notesInput = document.getElementById("notes-input");
let addBtn = document.getElementsByClassName("addBtn")[0];

function createNotes() {
  // getting value of notes from the text area
  let note = notesInput.value;
}
